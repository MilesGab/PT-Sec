'use client'
import { createTheme } from '@mui/material/styles';

const theme = createTheme({

    palette: {

        primary: {
            main: '#ffad33'
        },

        secondary: {
            main: '#6fa8dc'
        },
    },
});

export default theme;
